/* eslint-env browser */
import React from 'react';  //eslint-disable-line
import ReactDOM from 'react-dom';
import Component from 'Component';

ReactDOM.render(
  <Component first={12} second={13} />,
  document.getElementById('app-container')
);
